1. Insert into table WaitingList
================================
We changed the file insert.sql when it comes to WaitingList so that we only writte student names and the course. The attribut position is incremented automatically inside the table waitingList.
